{
  "name": "fs-mcp",
  "version": "0.4.2",
  "description": "Servidor MCP de FacturaScripts con soporte para múltiples instalaciones.",
  "author": {
    "name": "Daniel Fernández Giménez",
    "email": "contacto@danielfg.es",
    "url": "https://danielfg.es"
  },
  "license": "MIT",
  "keywords": ["FacturaScripts", "FS", "MCP", "ERP", "datos", "API", "Informes", "conexiones"],
  "skills": "./skills/",
  "mcpServers": {
    "facturascripts": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/dist/index.js"],
      "env": {
        "FS_CONNECTIONS_FILE": "${FS_CONNECTIONS_FILE}",
        "FS_DEFAULT_CONNECTION": "${FS_DEFAULT_CONNECTION}",
        "FS_LOCAL_MODULES_PATH": "${FS_LOCAL_MODULES_PATH}"
      }
    }
  },
  "userConfig": {
    "FS_CONNECTIONS_FILE": {
      "title": "Ruta del archivo de conexiones",
      "description": "Ruta absoluta al archivo connections.json con tus conexiones (ej: /Users/tu-usuario/.config/fs-mcp/connections.json). Si se deja vacío, se usará la carpeta interna del plugin (los datos no persisten entre sesiones en Cowork).",
      "type": "string",
      "sensitive": false
    },
    "FS_DEFAULT_CONNECTION": {
      "title": "Conexión por defecto",
      "description": "Clave de la conexión por defecto (ej: 'empresa1'). Puedes añadir conexiones con el skill 'fs-mcp:add-connection'.",
      "type": "string",
      "sensitive": false
    },
    "FS_LOCAL_MODULES_PATH": {
      "title": "Ruta de módulos locales privados",
      "description": "Ruta absoluta a la carpeta con tus módulos MCP privados (ej: /Users/tu-usuario/fs-mcp-modules-private). Si se deja vacío, no se cargarán módulos locales.",
      "type": "string",
      "sensitive": false
    }
  }
}
