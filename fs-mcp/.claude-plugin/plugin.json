{
  "name": "fs-mcp",
  "version": "1.1.0",
  "description": "Servidor MCP de FacturaScripts con soporte para múltiples instalaciones.",
  "author": {
    "name": "Daniel Fernández Giménez",
    "email": "contacto@danielfg.es",
    "url": "https://danielfg.es"
  },
  "license": "MIT",
  "keywords": ["FacturaScripts", "FS", "MCP", "ERP", "datos", "API", "Informes", "conexiones"],
  "skills": "./skills/",
  "mcpServers": {
    "facturascripts": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/server/dist/index.js"],
      "env": {
        "FS_CONNECTIONS_FILE": "${FS_CONNECTIONS_FILE}",
        "FS_DEFAULT_CONNECTION": "${FS_DEFAULT_CONNECTION}"
      }
    }
  },
  "userConfig": {
    "FS_CONNECTIONS_FILE": {
      "title": "Ruta del archivo de conexiones (override)",
      "description": "Solo necesario si quieres usar una ruta distinta a ~/.fs-claude.json. Normalmente déjalo vacío.",
      "type": "string",
      "sensitive": false
    },
    "FS_DEFAULT_CONNECTION": {
      "title": "Conexión por defecto (override)",
      "description": "Clave de la conexión por defecto. Normalmente se gestiona desde ~/.fs-claude.json con el skill 'fs-mcp:add-connection'.",
      "type": "string",
      "sensitive": false
    }
  }
}
